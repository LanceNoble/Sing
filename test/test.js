// import { strip } from "../index.js";
// import assert from 'node:assert/strict';

// describe("strip", function () {
//     it("should return [0, 0] when buf = [] and i = 0", function () {
//         assert.deepEqual(strip(Buffer.from([]), 0), [0, 0]);
//     });
//     it("should return [0, 0] when buf = [0x00, 0x4f, 0x67, 0x67, 0x53] and i = 0", function () {
//         assert.deepEqual(strip(Buffer.from([0x00, 0x4f, 0x67, 0x67, 0x53]), 0), [1, 0]);
//     })    
// });
